<% @page_title = "Game Name|Levels" %>

<div class="text-center center">
	<% @levels.each do |l| %>
		<div class="btn btn-default btn-lg btn-block">
			<%= link_to(l.level_no, :controller => "questions", :action => "index", :id => l.id)  %>
		</div>
	<% end %>
</div>