<% @subcategories.each do |sub| %>
	<div class="sub-concept">
		<%= link_to levels_index_path(:concept_id => sub.id), :class => "concept-btn", :data => {:id => sub.id} do %>
			<div class="sub-concept-name"><%= sub.name %></div>
		<% end %>
	</div>
<% end %>