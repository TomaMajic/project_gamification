<% @page_title = "Game Name|Learn" %>


<div class="center center2 concept">
	<% @concepts.each do |c| %>
		<div class="inline btn-concept category">
			<%= link_to levels_index_path(:concept_id => c.id), :class => "btn btn-default btn-lg btn-block concept-btn", :data => {:id => c.id} do %>
				<%= c.name %>
			<% end %>
		</div>
		<div class='p-bar animate inline'>
			<span style='width: 0%'></span>
		</div>
	<% end %>
</div>


